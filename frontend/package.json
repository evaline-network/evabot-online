{
  "name": "evabot-frontend",
  "version": "0.0.1",
  "description": "EvaBot Online Frontend — TypeScript + Vite cyber-terminal consuming the Python FastAPI backend",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^5.0.0",
    "jsdom": "^30.0.1",
    "typescript": "^5.7.3",
    "vite": "^6.0.0",
    "vitest": "^5.0.0"
  }
}
